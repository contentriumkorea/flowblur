# GitHub 업로드

이 폴더가 저장소 루트입니다. GitHub에 빈 저장소를 만든 후 이 폴더에서 실행하세요. URL은 본인 저장소 주소로 바꿉니다.

```sh
git add .
git commit -m "Initial FlowBlur native effect source"
git remote add origin https://github.com/YOUR_ACCOUNT/flowblur.git
git push -u origin main
```

원격 업로드는 아직 수행하지 않았습니다. 공개 여부와 라이선스는 소유자가 결정합니다. Adobe SDK를 git add -f 등으로 추가하지 마세요. 바이너리는 SDK 배포 조건을 확인한 뒤 GitHub Release 첨부 여부를 결정하세요.
